local UNESCAPED_STRING = "string doesnt have delimiter"
local UNKNOWN_NUMBER = "unknown error while converting source to number"
local UNKNOWN_CHARACTER = "unknown character"

return {
	UNESCAPED_STRING = UNESCAPED_STRING,
	UNKNOWN_NUMBER = UNKNOWN_NUMBER,
	UNKNOWN_CHARACTER = UNKNOWN_CHARACTER,
}
